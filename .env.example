# PostgreSQL
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_secure_postgres_password
POSTGRES_DB=digi_notes
POSTGRES_PORT=5432

# Redis
REDIS_PORT=6379
REDIS_PASSWORD=

# Service ports
BACKEND_PORT=5000
AI_SERVICE_PORT=8000

# Docker
COMPOSE_PROJECT_NAME=digi-notes

# Note: Copy this file to .env and update values before running docker-compose.

# --- AWS Neptune configuration ---
# Neptune endpoint host (required)
NEPTUNE_ENDPOINT=
# Neptune port (required)
NEPTUNE_PORT=8182
# Use IAM auth (true) or static username/password (false)
NEPTUNE_USE_IAM=true
# Static auth credentials (leave empty for IAM)
NEPTUNE_USERNAME=
NEPTUNE_PASSWORD=
# Connection pool size for Gremlin client
NEPTUNE_POOL_SIZE=10
# Health check timeout in seconds
NEPTUNE_HEALTH_CHECK_TIMEOUT=5
# Retry configuration for Neptune operations
NEPTUNE_RETRY_ATTEMPTS=3
NEPTUNE_RETRY_MULTIPLIER=2
NEPTUNE_RETRY_MAX_WAIT=10

# Graph visualization limits
GRAPH_VISUALIZE_MAX_NODES=500
GRAPH_VISUALIZE_MAX_EDGES=1000

# Neptune Authentication: Set NEPTUNE_USE_IAM=true for IAM auth (recommended for AWS),
# or provide NEPTUNE_USERNAME/NEPTUNE_PASSWORD for static auth (local dev).

# --- Summarization / OpenAI settings ---
SUMMARIZER_BRIEF_MAX_TOKENS=500
SUMMARIZER_DETAILED_MAX_TOKENS=1500
SUMMARIZER_TEMPERATURE=0.3

# --- Summarization Cache (Redis) ---
REDIS_CACHE_ENABLED=true
# Cache TTL for summaries (seconds). Default 3600 = 1 hour.
REDIS_CACHE_TTL=3600

# Top-level orchestration notes:
# - This file is mainly used by docker-compose to set common environment variables.
# - Override service-specific variables in `backend/.env`, `ai/.env`, and `frontend/.env` when running locally.

# Docker tips:
# - Use `COMPOSE_PROJECT_NAME` to isolate resources when running multiple projects.
# - For local dev, expose Postgres and Redis as separate containers; for production use managed services.

# Neptune note:
# - For AWS Neptune, prefer IAM auth and set `NEPTUNE_USE_IAM=true` with proper IAM roles assigned to the runtime.