# Frontend environment example for Vite
# Copy to `.env` (local only) and do NOT commit secrets to VCS.
# Vite exposes only variables that start with VITE_

# `VITE_API_URL`: Base URL for backend API (include /api suffix if your backend expects it)
VITE_API_URL=http://localhost:5000/api

# Polling interval used by UI to check processing status (ms). Lower values increase load.
VITE_POLLING_INTERVAL_MS=2000

# UI-side upload limit hint (actual enforcement is server-side)
VITE_MAX_UPLOAD_SIZE_MB=10

# Feature flags (example)
# VITE_ENABLE_BETA_FEATURE=true

# Troubleshooting:
# - Ensure backend CORS allows the `VITE_API_URL` origin.
# - For production, configure environment injection in your CI/CD or hosting platform.
VITE_API_URL=http://localhost:5000